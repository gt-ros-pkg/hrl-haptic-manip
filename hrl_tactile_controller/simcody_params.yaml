haptic_mpc:
  simcody:
    # Skin topic list. Each must publish
    skin_list: ['/skin/taxel_array']

  #  wp_goal_tf_frame: '/world'
    torso_frame: '/torso_lift_link'
    inertial_frame: '/world'

    jep_start: [deg(0.0), deg(0.0), deg(0.0), deg(10.0), deg(0.0), deg(10.0), deg(0.0)]
    planar: true

